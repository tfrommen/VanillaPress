/**
 * Main app file. Initializes app components.
 */

/**
 * The main app object.
 * @namespace
 */
var vanillaPress = {
	/**
	 * Initializes the app.
	 */
	init: function() {
	}
};

// Initialize the app.
vanillaPress.init();
